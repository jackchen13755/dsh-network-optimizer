# dsh-web-network-optimizer bundle patch:one row over the web profile.
# The row name resolves from the profile's own node_modules where
# `dsh plugin --profile web add <path>` installed this package.
- insert:
    - id: web-optimizer
      name: 'dsh-web-network-optimizer'
